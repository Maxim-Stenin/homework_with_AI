# Журнал инструментов

> Только дополняется, старые записи не редактируются. Сюда попадает всё, что вы
> добавили в проект или окружение ради этой задачи: библиотеки, CLI-утилиты,
> линтеры, MCP-серверы, плагины и скиллы агента, хуки, внешние сервисы, контейнеры.
>
> Разовый вызов уже установленного инструмента сюда не пишется — он идёт
> в таблицу инструментов внутри файла сессии.
>
> Тупиковая попытка — тоже запись: поставили, не подошло, сняли. Это как раз интересно.

## Шаблон записи

```markdown
## YYYY-MM-DD · Сессия N · <инструмент> vX.Y

- **Тип:** библиотека / CLI / MCP / плагин / скилл / хук / сервис
- **Установка:** `<точная команда>`
- **Зачем:** какую задачу решает
- **Область:** проект или глобально
- **Проверка:** чем убедились, что работает
```

Удалённый или заменённый инструмент фиксируется так же — с пометкой
**удалён** / **заменён на X** и причиной.

---

## 2026-08-13 · Сессия 1 · git 2.55.0.windows.4

- **Тип:** CLI
- **Установка:** уже присутствовал в системе, не ставили
- **Зачем:** репозиторий под сдачу; журнал и код должны лежать вместе, чтобы была
  видна история работы
- **Область:** проект
- **Проверка:** `git --version` → `git version 2.55.0.windows.4`;
  `git init -b main` → `Initialized empty Git repository`;
  `git add -A` → 17 файлов в индексе

## 2026-08-13 · Сессия 1 · GitHub CLI 2.97.0

- **Тип:** CLI
- **Установка:** `winget install --id GitHub.cli --accept-source-agreements --accept-package-agreements --disable-interactivity`
- **Зачем:** создать репозиторий и в дальнейшем работать с PR/issue, не уходя из сессии
- **Область:** глобально
- **Проверка:** `gh --version` → `gh version 2.97.0 (2026-07-31)`.
  `gh auth status` → `You are not logged into any GitHub hosts`. **Авторизация не выполнена
  и агентом выполняться не будет:** ввод паролей и токенов делает человек.

## 2026-08-13 · Сессия 1 · Извлечение текста PDF через PowerShell

- **Тип:** разовый скрипт, не инструмент проекта
- **Установка:** не устанавливался; скрипт живёт во временной папке сессии, в репозиторий
  не попадает
- **Зачем:** прочитать `homework.pdf` — в системе нет ни Python, ни poppler-utils,
  встроенное чтение PDF не работает
- **Область:** временно, только сессия 1
- **Проверка:** извлечено 1079 строк осмысленного текста задания, разделы сошлись
  с `README.md`. **Ограничение:** моноширинный шрифт использует другую карту глифов,
  имена файлов в извлечённом тексте искажены (`?HAQDA.id` вместо `CLAUDE.md`) —
  восстанавливались по контексту

## 2026-08-13 · Сессия 1 · Запреты в `.claude/settings.json`

- **Тип:** конфигурация разрешений (механизм под правила из `AGENTS.md`)
- **Установка:** создан файл `.claude/settings.json` с `permissions.deny` на необратимые
  команды (`git push --force`, `git reset --hard`, `git clean`, `git filter-branch`,
  удаление веток и тегов, `rm -rf`, `Remove-Item -Recurse`) и на правку `materials/`
  и `homework.pdf`; `permissions.ask` на коммиты, push и установку зависимостей
- **Область:** проект
- **Проверка: провалена.** Контрольный запуск заведомо запрещённой команды `git clean -n`
  (сухой прогон) **выполнился вместо блокировки**. Файл записан верно, но конфигурация
  не подхвачена: папки `.claude/` не существовало на старте сессии, наблюдатель за
  настройками её не отслеживает. Требуется перезапуск сессии и повторный контрольный
  запуск — до этого запреты считаются недействующими. Отмечено в `STATE.md`
  как известная проблема.

## Планировалось, но отложено (сессия 1)

- **Playwright MCP** — не ставили. Задание засчитывает прогон Playwright наравне с ручной
  проверкой клавиатурой, а на настройку закладывает 30–50 минут плюс отдельный подвох
  с заблокированным `file://`. Ставить под объект, которого ещё нет, — потратить время
  дважды. Решение пересматривается в сессии 2.
- **Проектные скиллы QA** (формат баг-репорта, шаблон тест-плана) — не создавали.
  Формат баг-репорта пока живёт в `AGENTS.md`; выносить его в скилл имеет смысл,
  когда станет понятен стек объекта.

## 2026-08-13 · Сессия 3 · Python 3.14.7

- **Тип:** среда выполнения (нужна только ради стенда)
- **Установка:** установлена человеком вручную с python.org,
  путь `C:\Users\msten\AppData\Local\Programs\Python\Python314`
- **Зачем:** поднять статический стенд командой `python -m http.server 8080 --directory app`.
  `file://` под браузерными агентами заблокирован, значит объект нужно раздавать по HTTP.
  Выбран встроенный `http.server`, а не Node или сторонний сервер: раздача одного статического
  файла не стоит новой зависимости, а Node в системе всё равно нет.
- **Проверка: пройдена.** `python --version` → `Python 3.14.7`; стенд поднят,
  `Invoke-WebRequest` к `http://127.0.0.1:8080/index.html` вернул `HTTP 200 OK`, 20014 байт;
  страница открыта в браузерной панели, заголовок читается как «Мои финансы».
- **Подвох, стоивший времени.** В PATH лежит заглушка Python из Microsoft Store
  (`WindowsApps\python.exe`): на `--version` она отвечает словом `Python` без номера
  и кодом выхода 49. Выглядит как установленный интерпретатор, но им не является.
  Настоящий Python не попал в PATH этой сессии, потому что сессия стартовала раньше
  установки, — вызывался по абсолютному пути. Признак заглушки: **версия без номера**.

## 2026-08-13 · Сессия 3 · Запреты `.claude/settings.json` — перепроверка

- **Тип:** конфигурация разрешений
- **Установка:** к существующему `permissions.deny` добавлены `Edit(app/**)`,
  `Write(app/**)`, `NotebookEdit(app/**)` — механизм под главный запрет роли тестировщика
- **Проверка: пройдена, провал сессии 1 закрыт.** Две контрольные пробы:
  1. Попытка записать `app/__deny_probe.txt` → `File is in a directory that is denied
     by your permission settings`. Проба сделана новым файлом, а не правкой `index.html`:
     если бы запрет опять не сработал, объект всё равно остался бы нетронутым.
  2. Та самая команда, что прошла в сессии 1, — `git clean -n` → `Permission to use Bash
     with command git clean -n has been denied`.
- **Вывод:** гипотеза сессии 1 подтвердилась — дело было в отсутствии папки `.claude/`
  на старте сессии, а не в неверном синтаксисе правил. После перезапуска конфигурация
  подхватывается, причём новые правила действуют сразу, без перезапуска.
  Запреты перестали быть только текстом.

## 2026-08-13 · Сессия 4 · Скиллы `qa-manual` и `qa-autotest`

- **Тип:** скиллы агента, проектные
- **Установка:** написаны вручную, ничего не ставилось:
  `.claude/skills/qa-manual/SKILL.md` и `.claude/skills/qa-autotest/SKILL.md`
- **Зачем:** `qa-manual` — порядок ручной проверки: наивный прогон, формат `TC-NNN`,
  правило «проверять отсутствие записи, а не только сообщение об ошибке», что считать
  дефектом, а что замечанием. `qa-autotest` — структура `tests/`, фикстуры, порядок
  предпочтения селекторов по существующей разметке и главное правило «тест на дефекте
  обязан падать; чинится тест, но никогда не объект».
- **Область:** проект — едут в репозиторий вместе со сдачей, в отличие от глобальных
- **Почему свои, а не готовые:** поиск по доступным скиллам (ручное тестирование,
  тест-план, баг-репорты, Playwright, exploratory) вернул **пустой результат**.
  Готовых нет. Задним числом это к лучшему: внешний скилл принёс бы свой формат
  баг-репорта, который разошёлся бы с форматом из `AGENTS.md`.
- **Проверка: не выполнена на момент записи.** Файлы созданы, но ни один скилл ещё
  не срабатывал в деле — проверкой будет первая сессия тестирования: подхватился ли
  скилл по описанию и повлиял ли на поведение агента. До этого считать непроверенным.
- **Дополнение той же сессии — скиллы расширены по требованию человека** («сделать
  количество проверок и глубину выше»). Добавлены справочники:
  `qa-manual/references/checklist.md` — каталог из 186 проверок по 14 областям
  с типом (позитивная / негативная / граничная) и ссылкой на пункт `SPEC.md`;
  `qa-autotest/references/patterns.md` — рецепты кода: фикстуры стенда и чистого
  хранилища, параметризация классов эквивалентности, диалоги подтверждения, выгрузка
  и загрузка файлов, инварианты, тесты на дефектах.
  В сами `SKILL.md` добавлены разделы про техники проектирования проверок, нормы объёма
  и глубины, слои проверок и детерминизм. Справочники вынесены отдельными файлами
  намеренно: подгружаются по ссылке, а не раздувают скилл при каждом срабатывании.

## 2026-08-13 · Сессия 4 · Раннер автотестов — решение принято, установка не выполнена

- **Тип:** библиотека (pytest + Playwright для Python)
- **Установка:** `pip install pytest pytest-playwright` и
  `python -m playwright install chromium` — **пока не запускались**. Человек потребовал
  спрашивать перед каждой командой установки отдельно; ставить в этой сессии было нечего,
  тестов ещё нет.
- **Зачем:** закрывает `TODO(сессия 3)` — падающий автотест на найденном дефекте
  требуется заданием
- **Отклонённая альтернатива — Selenium.** Узнаваемее и легче ставится (драйвер
  подтягивается сам), но ожидание элементов не встроено: объект перерисовывает таблицу
  и диаграмму мгновенно на JS, и без вручную расставленных `WebDriverWait` тест начинает
  мигать. Сдаётся **падающий** тест, и он должен падать по дефекту, а не через раз
  по таймингу.
- **Проверка:** будет при первом прогоне `python -m pytest tests -v`

## 2026-08-13 · Сессия 4 · MCP — рассмотрен и отклонён

- **Тип:** MCP-сервер
- **Установка:** не выполнялась
- **Что рассматривалось:** Playwright MCP (Microsoft) и Chrome DevTools MCP (Google).
  Оба требуют Node.js, которого в системе нет (проверено в сессии 2).
- **Почему отклонено:** человек счёл установку Node дороже пользы. Браузер агента
  остаётся встроенной панелью, которой пользовались в сессиях 2 и 3, — она умеет
  клики, чтение разметки и снимки экрана.
- **Отдельно про Selenium как MCP:** официального сервера у проекта нет. Сторонние
  обёртки неизвестного авторства, получающие доступ к браузеру и файлам, в проект
  под сдачу не подключаются.
- **Последствие, которое нельзя замалчивать:** из «трёх китов» занятия задействованы
  только скиллы. MCP и хуки остаются незадействованными — повторить в финальном отчёте.
