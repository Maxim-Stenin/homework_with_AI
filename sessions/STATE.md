# Состояние проекта

> Обновляется в конце каждой сессии. Описывает «как есть сейчас», а не историю
> изменений — старое перезаписывается. Первым делом в новой сессии агент читает
> именно этот файл.

**Обновлено:** 2026-08-13, сессия 1

## Направление и объём

Направление — **QA** (задание 4: проверка продукта, который сделал агент).
Срок сдачи: **17 августа 2026, 18:00**. Осталось 4 дня.

## Готово

- Задание прочитано целиком (извлечено из `homework.pdf`), требования и состав сдачи
  разобраны.
- Репозиторий инициализирован: `git init -b main`, создан `.gitignore`.
- `gh` CLI 2.97.0 установлен.
- `AGENTS.md`, Часть II — заполнена: две роли (автор / тестировщик) с границей между ними,
  категоричные запреты, условия остановки, формат баг-репорта, чек-лист перед финальным
  ответом, соглашения проекта.
- `sessions/session-1.md` — заполнен.
- `sessions/TOOLS.md` — записи по git, gh, извлечению PDF и запретам, включая отложенное.

- **Связь с GitHub настроена.** `origin` →
  `https://github.com/Maxim-Stenin/homework_with_AI.git`, коммит `c361225` запушен,
  `main` отслеживает `origin/main`.

## В работе

- Ничего не начато и не брошено на середине.

## Не начато

- `SPEC.md` — пустой шаблон. Заполняется в сессии 2 вопросами агента и ответами человека.
- Объект тестирования (маленькое web-приложение) — не создан, тема и стек не выбраны.
- Наивный прогон — не делался.
- Тест-план, баг-репорты, автотест, протокол расхождений — не начаты.
- Аудит другой моделью или чистой сессией — не проводился.
- `README.md` под сдачу — пока лежит исходный файл из комплекта, не переписан.

## Активные инструменты

- git 2.55.0.windows.4 — репозиторий, ветка `main`.
- GitHub CLI 2.97.0 — установлен, **но не авторизован**: `gh auth status` сообщает
  «not logged into any GitHub hosts», конфигов на диске нет. Авторизация прошла в браузере
  на github.com, push работает через Git Credential Manager. Пока `gh` не даёт ничего
  сверх обычного git — окупится, только если понадобятся PR и issue из сессии.
- **MCP, скиллы и хуки — не настроены.** Ни один из «трёх китов» занятия не задействован.
- Claude Code, модель Opus 5, режим «спрашивать перед правками».
- Playwright MCP и проектные скиллы QA — сознательно отложены, см. `TOOLS.md`.

## Известные проблемы

- **Запреты в `.claude/settings.json` не действуют.** Контрольный запуск заведомо
  запрещённой команды `git clean -n` выполнился вместо блокировки. Наиболее вероятная
  причина: папки `.claude/` не существовало на старте сессии, и наблюдатель за настройками
  её не подхватил. **Пока это не перепроверено после перезапуска, запреты живут только
  в тексте `AGENTS.md`, то есть держатся на дисциплине, а не на механизме.**
- Часть II `AGENTS.md` содержит незаполненные места, помеченные `TODO(сессия 2)`:
  стек объекта, команды запуска стенда, порт, раннер автотестов, папка объекта.
  Пока пометка на месте — решение не принято.
- Текст задания извлечён из PDF самодельным скриптом. Моноширинные фрагменты
  (имена файлов, команды, ссылки) искажены и восстанавливались по контексту —
  есть риск неверно прочитанной детали именно в них.
- Конфликт в постановке разрешён допущением: пет-проект человека и объект тестирования —
  разные вещи. Объект создаётся отдельной сессией и руками не правится. Если допущение
  неверно, переигрывать нужно до наивного прогона.

## Следующий шаг

**Новая сессия.** Первым делом — контрольный запуск запрещённой команды (`git clean -n`):
если после перезапуска она блокируется, запреты действуют; если нет — разбираться
с конфигурацией, потому что без механизма правила ничего не удерживают.
Затем — заполнение `SPEC.md` вопросами агента и ответами человека.
