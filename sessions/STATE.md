# Состояние проекта

> Обновляется в конце каждой сессии. Описывает «как есть сейчас», а не историю
> изменений — старое перезаписывается. Первым делом в новой сессии агент читает
> именно этот файл.

**Обновлено:** 2026-08-13, сессия 3

## Направление и объём

Направление — **QA** (задание 4: проверка продукта, который сделал агент).
Срок сдачи: **17 августа 2026, 18:00**. Осталось 4 дня.

## Готово

- Задание прочитано целиком (извлечено из `homework.pdf`), требования и состав сдачи
  разобраны.
- Репозиторий инициализирован: `git init -b main`, создан `.gitignore`.
- `gh` CLI 2.97.0 установлен.
- `AGENTS.md`, Часть II — заполнена: две роли (автор / тестировщик) с границей между ними,
  категоричные запреты, условия остановки, формат баг-репорта, чек-лист перед финальным
  ответом, соглашения проекта.
- `sessions/session-1.md` — заполнен.
- `sessions/TOOLS.md` — записи по git, gh, извлечению PDF и запретам, включая отложенное.

- **Связь с GitHub настроена.** `origin` →
  `https://github.com/Maxim-Stenin/homework_with_AI.git`, коммит `c361225` запушен,
  `main` отслеживает `origin/main`.

- **Объект тестирования создан** — `app/index.html`, сессия 2, роль Автор.
  Учёт личных финансов: доходы и расходы, кольцевая диаграмма по категориям,
  таблица истории, выгрузка и загрузка CSV. Один файл, чистые HTML/CSS/JS,
  зависимостей и сборки нет, открывается двойным кликом.
  **Код объекта дальше не правится ни при каких обстоятельствах.**
- `sessions/session-2.md` — заполнен.

- **Правила тестировщика дописаны (сессия 3).** В `AGENTS.md`: команда стенда, адрес и порт,
  разделы «Объём проверки», «Целевое окружение», развёрнутый запрет на правку объекта
  с перечнем лазеек, дополненные условия остановки.
- **Стенд работает и проверен.** Python 3.14.7 установлен человеком,
  `python -m http.server 8080 --directory app` → `HTTP 200 OK`, 20014 байт; страница
  открывается в браузерной панели, кодировка цела.
- **Запреты стали механизмом.** В `deny` добавлены `Edit(app/**)`, `Write(app/**)`,
  `NotebookEdit(app/**)`. Две контрольные пробы заблокированы.
- **Связь с GitHub перепроверена:** `git ls-remote --heads origin` вернул `5c565d6`,
  совпадает с локальным HEAD; `main` отслеживает `origin/main`, дерево чистое.
- `sessions/session-3.md` — заполнен.

## В работе

- Ничего не начато и не брошено на середине.

## Не начато

- `SPEC.md` — пустой шаблон. Порядок изменился: объект создан раньше спецификации,
  по короткому промпту. Заполнять `SPEC.md` «под готовое» нельзя — тогда описание
  и реализация сойдутся по определению. Вносить туда исходный промпт как есть
  и от него считать расхождения.
- Наивный прогон — не делался.
- Тест-план, баг-репорты, автотест, протокол расхождений — не начаты.
- Аудит другой моделью или чистой сессией — не проводился.
- `README.md` под сдачу — пока лежит исходный файл из комплекта, не переписан.

## Активные инструменты

- git 2.55.0.windows.4 — репозиторий, ветка `main`.
- GitHub CLI 2.97.0 — установлен, **но не авторизован**: `gh auth status` сообщает
  «not logged into any GitHub hosts», конфигов на диске нет. Авторизация прошла в браузере
  на github.com, push работает через Git Credential Manager. Пока `gh` не даёт ничего
  сверх обычного git — окупится, только если понадобятся PR и issue из сессии.
- **MCP, скиллы и хуки — не настроены.** Ни один из «трёх китов» занятия не задействован.
- Claude Code, модель Opus 5, режим «спрашивать перед правками».
- Playwright MCP и проектные скиллы QA — сознательно отложены, см. `TOOLS.md`.

## Известные проблемы

- ~~Запреты в `.claude/settings.json` не действуют.~~ **Закрыто в сессии 3.** Гипотеза
  подтвердилась: дело было в отсутствии папки `.claude/` на старте сессии 1. После
  перезапуска запреты работают, две контрольные пробы заблокированы (`Write(app/**)`
  и `git clean -n`). Новые правила подхватываются сразу, без перезапуска.
- **В `AGENTS.md` остался один живой `TODO(сессия 3)` — раннер автотестов.** Остальные
  пометки закрыты в сессии 3 (стек объекта, папка `app/`, команда стенда, порт 8080).
  Раннер не выбран сознательно: решение человека, агент за него не проставляет.
  **Пока пометка на месте, автотест на дефекте писать не на чем** — а он требуется заданием.
- **Вне объёма проверки по решению человека:** сохранность `localStorage` (перезагрузка,
  две вкладки, порча хранилища), клавиатурная навигация и доступность, кросс-браузерность.
  Первое рискованнее прочего: всё состояние приложения живёт в `localStorage`.
  Записано в `AGENTS.md`, повторить в финальном отчёте.
- **Расхождение описания и реализации, известное с момента создания объекта:**
  «история транзакций пишется в CSV-файл» — файл появляется только по нажатию
  «Сохранить CSV», сама по себе история живёт в `localStorage`. Браузер не может
  писать на диск без участия человека. В сессии тестирования завести как расхождение,
  а не списать на «так и задумано».
- В `AGENTS.md` недостаточно явно разведены роли в правиле «останавливайся и спрашивай».
  По первому промпту сессии 2 агент остановился с вопросами о деталях объекта —
  формально по правилу, по сути неверно: договорённости об углах убивают материал
  для находок. Правило работает для роли тестировщика, не для создания объекта.
- Текст задания извлечён из PDF самодельным скриптом. Моноширинные фрагменты
  (имена файлов, команды, ссылки) искажены и восстанавливались по контексту —
  есть риск неверно прочитанной детали именно в них.
- Конфликт в постановке разрешён допущением: пет-проект человека и объект тестирования —
  разные вещи. Объект создаётся отдельной сессией и руками не правится. Если допущение
  неверно, переигрывать нужно до наивного прогона.

## Следующий шаг

**Наивный прогон.** Окружение готово: стенд поднимается, правила и запреты на месте.
Поднять `http://127.0.0.1:8080/index.html`, пройти основной сценарий без тест-плана
и записать первое впечатление в `naive-run/`. Затем — тест-план (минимум треть проверок
негативные и граничные) и баг-репорты.

Параллельно требует решения человека: **раннер автотестов** и заполнение `SPEC.md`
исходным промптом как есть.

Заготовка находок, которые видны уже автору и требуют проверки тестировщиком:
запись CSV только по кнопке; категория — свободный текст, «Кафе» и «кафе» дадут
две разные категории; «Загрузить CSV» заменяет список целиком без подтверждения;
дата будущим числом проходит.
